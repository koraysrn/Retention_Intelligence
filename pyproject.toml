[build-system]
requires = ["setuptools>=84.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "churn-reengagement-platform"
version = "0.1.0"
description = "Enterprise churn prediction, A/B experiment platform and Agentic AI/RAG re-engagement system"
readme = "README.md"
requires-python = ">=3.12,<4"
license = { text = "Proprietary" }
authors = [{ name = "Re-Engagement Platform Team" }]
dependencies = [
    # Core data processing
    # NOTE: pandas 3.x is not yet supported by mlflow 3.15.1 (mlflow imposes a
    # pandas<3 constraint). The latest pandas 2.x release (2.3.3) is used for
    # full-stack compatibility.
    "pandas>=2.3.3,<4",
    "numpy>=2.5.2",
    "pyarrow>=25.0.1",
    "duckdb>=1.5.5",
    "pyyaml>=6.0.3",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "python-dotenv>=1.2.3",
]

[project.optional-dependencies]
ml = [
    "scikit-learn>=1.9.0",
    "lightgbm>=4.7.0",
    "xgboost>=3.4.1",
    "catboost>=1.2.10",
    "imbalanced-learn>=0.14.0",
    "shap>=0.52.0",
    "matplotlib>=3.11.1",
    "seaborn>=0.13.2",
]
experiment = [
    "scipy>=1.18.0",
    "statsmodels>=0.14.6",
]
serving = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]
mlops = [
    "mlflow>=3.15.1",
]
monitoring = [
    "evidently>=0.7.21",
]
agents = [
    "langgraph>=1.2.11",
    "langchain>=1.3.16",
    "langchain-openai>=1.6.0",
    "langchain-anthropic>=1.6.1",
    "langchain-community>=0.4.2",
    "psycopg[binary]>=3.3.4",
    "pgvector>=0.5.0",
]
dbt = [
    "dbt-core>=1.12.2",
    "dbt-duckdb>=1.11.0",
]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.4",
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
]

[project.scripts]
churn-train = "src.models.train:main"
churn-score = "src.serving.batch_score:main"
churn-api = "src.serving.api:run"
ab-analyze = "src.experiments.analysis:main"
agent-run = "src.agents.orchestrator:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["src", "scripts", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.14"
warn_return_any = true
warn_unused_ignores = true
ignore_missing_imports = true
exclude = ["scripts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
filterwarnings = [
    "ignore::PendingDeprecationWarning:shap",
]
markers = [
    "slow: slow tests",
    "integration: tests requiring external services",
]

[tool.coverage.run]
source = ["src"]
omit = ["src/serving/api.py"]
